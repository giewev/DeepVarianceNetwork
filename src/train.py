import model
import util
import data

def train(model, training_data, test_data, num_epochs,batch_size):
	raise NotImplemented


if __name__ == '__main__':
	raise NotImplemented
	# Read arguments or use defaults

	# Check if the raw comments are already processed into data
	# If they are, load them
	# If not, process them and serialize them

	# Load existing model if it exists and "Restart" parameter is false
	# Otherwise initialize a new model

	# Train the model
	# Save the model